# Convenience union for local dev, so one venv can run both services and the
# full test suite. Each service installs only its own slice when deployed:
# see solver/requirements.txt (Render) and studio/requirements.txt (Vercel).
-r solver/requirements.txt
-r studio/requirements.txt
